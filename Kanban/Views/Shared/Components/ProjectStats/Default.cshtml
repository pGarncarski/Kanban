@using Kanban.Models
@using System.Collections.Generic

@model Dictionary<IssueState, int>

<div>Aktualny status zadań:</div>
<ul>
    @foreach (var keyValue in Model)
    {
        <li>@Html.DisplayFor(m => keyValue.Key) - @keyValue.Value </li>
    }
</ul>